@model CakeDessertShop.Models.AddProductImageViewModel

@{
    ViewData["Title"] = "Adicionar imagen";
}

<h1>Adicionar imagen</h1>

<hr />
<div class="row">
    <div class="col-md-12">
        <form asp-action="AddImage" enctype="multipart/form-data">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="ProductId" />
            <div class="form-group">
                <label asp-for="ImageFile" class="control-label"></label>
                <input asp-for="ImageFile" class="form-control" type="file" />
                <span asp-validation-for="ImageFile" class="text-danger"></span>
            </div>
            <div class="form-group mt-2">
                <input type="submit" value="Adicionar" class="buttonmiddle" />
                <a asp-action="Details" asp-route-id="@Model.ProductId" class="buttonlight">Regresar</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
